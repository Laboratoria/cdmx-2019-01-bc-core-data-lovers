// require('../src/data.js');


describe('pokemon', () => {
  it('is a function', () => {
    expect(typeof pokemon).toBe('object');
  });

  // it('returns `example`', () => {
  //   expect(example()).toBe('example');
  // });
});
