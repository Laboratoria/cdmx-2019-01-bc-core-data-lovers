// require('../src/data.js');


describe('data', () => {
  it('is a function', () => {
    expect(typeof data).toBe('object');
  });

  // it('returns `example`', () => {
  //   expect(example()).toBe('example');
  // });
});
