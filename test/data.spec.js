require('../src/data.js');

describe('data', () => {
  it('Debería devolver un objeto', () => {
    expect(typeof data).toBe('object');
  });

//   it('returns `example`', () => {
//     expect(example()).toBe('example');
//   });
});
